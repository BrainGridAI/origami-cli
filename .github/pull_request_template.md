## What and why

<!-- One or two sentences: the change and the problem it solves. Link the issue if there is one. -->

## Checklist

- [ ] `pnpm typecheck && pnpm test && pnpm build` pass locally
- [ ] Behavior changes have a test
- [ ] `src/v3/catalog.ts` is only changed by `pnpm sync:v3`, never by hand
- [ ] README / CHANGELOG updated if users will notice
